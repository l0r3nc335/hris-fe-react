# Leave empty in local dev so /api requests use the Vite proxy (required for auth cookies + CSRF).
VITE_API_BASE_URL=
# Dev-only Vite proxy target for /api requests.
VITE_DEV_PROXY_TARGET=http://localhost:3001
VITE_APP_ENV=development
VITE_SENTRY_DSN=
